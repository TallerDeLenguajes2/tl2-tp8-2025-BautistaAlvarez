@model tl2_tp8_2025_BautistaAlvarez.ViewModels.AgregarProductoViewModel
@{
ViewData["Title"] = "Agregar Producto al Presupuesto";
}

<h2>Agregar Producto al Presupuesto Nro @Model.IdPresupuesto</h2>
<hr />

<div class="row">
    <div class="col-md-6">
        <form asp-action="AgregarProducto"><!--El action agregarProducto-->
            <input type="hidden" asp-for="IdPresupuesto" /><!--Este id es el que mando para no perderlo-->
            <div class="form-group">
                <label asp-for="IdProducto" class="control-label"></label><!--Aqui el usuario llena-->
                <select asp-for="IdProducto" asp-items="@Model.ListaProductos"class="form-control"><!--Aqui agrego el select listaporducto del model para que el usuario elija-->
                    <option value="">-- Seleccionar Producto --</option>
                </select>
                <span asp-validation-for="IdProducto" class="text-danger"></span><!--Validacion, agregar-->
            </div>
            <div class="form-group">
                <label asp-for="Cantidad" class="control-label"></label><!--Aqui el usuario llena-->
                <input asp-for="Cantidad" class="form-control" />
                <span asp-validation-for="Cantidad" class="text-danger"></span><!--Validacion, agregar-->
            </div>
            <div class="form-group mt-4">
                <input type="submit" value="Añadir Producto" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>
<div class="mt-3">
    <a asp-action="Details" asp-route-id="@Model.IdPresupuesto">Volver al Presupuesto</a>
</div>
<!--Agregar-->
@section Scripts {
<partial name="_ValidationScriptsPartial" />
}